# v3.2 Patch Notes
- Fix TypeScript build error: missing types for xml2js
- Add `@types/xml2js` to devDependencies
- Add fallback declaration under `types/xml2js.d.ts`
- Improve Tailwind `content` globs to avoid empty CSS warning
